# Interface and port for the host listener (required)
INTERFACE=127.0.0.1
PORT=5000

# Default SOURCE_PATHS example: mix of read-only and read-write mounts with aliases
# Grammar: /host/path[:ro|:rw][@Alias]
# - Permission defaults to :ro
# - Alias defaults to basename(path)
SOURCE_PATHS=/home/dev/src:rw@App,/var/log:ro@Logs,/data/shared@Shared

# Container image and naming
IMAGE=gitpod/openvscode-server:latest
CONTAINER_NAME=ovscode_plug

# Auto-detect container engine (docker preferred). Override to force one.
#CONTAINER_ENGINE=docker

# Auto-detected from the first valid SOURCE_PATH; set explicitly if needed.
#USERMAP=1000:1000

# Connection token: leave empty to auto-generate and persist in .ovscode_plug/.env.runtime
#CONNECTION_TOKEN=

# Uncomment to run in strict offline mode (no outbound network)
#OFFLINE_MODE=true

# Uncomment to run with a read-only root filesystem (extensions requiring writes will break)
#READONLY_CONTAINER=true

# Optional base path (e.g., for reverse proxies). Must begin with a slash, no trailing slash.
#BASE_PATH=/clienteA

# SELinux: append label to mount options (e.g., :z or :Z)
#MOUNT_LABEL=:z

# Choose strict or auto port strategy. Auto finds the next open port if the requested one is busy.
#PORT_STRATEGY=auto

# File to tee runtime logs (optional)
#LOG_FILE=/var/log/openvscode-runtime.log

# Dry-run to preview the plan without starting a container
#DRY_RUN=true

# Example subpath configuration (uncomment and adjust)
#INTERFACE=127.0.0.1
#PORT=5100
#BASE_PATH=/clienteA
#SOURCE_PATHS="/srv/clienteA/code:rw@clienteA_Code,/srv/common/docs@Docs"

# Example SELinux-friendly mount
#MOUNT_LABEL=:z
#SOURCE_PATHS="/srv/selinux/app:rw@AppZ,/srv/selinux/logs:ro@LogsZ"
